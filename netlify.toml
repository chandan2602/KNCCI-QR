[build]
  command = "npm run build"
  publish = "dist/Lmsapp"

[build.environment]
  NODE_VERSION = "18"
  NODE_OPTIONS = "--openssl-legacy-provider --max_old_space_size=6048"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
